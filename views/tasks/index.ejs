<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All tasks</title>
</head>
<body>
    <header>
        <h1>Task manager</h1>
    </header>

    <main>
        <section>
            <h2>List of tasks</h2>
            <% if (locals.tasks) { %>
            <ul>
                <% for( let task of tasks ) { %>
                    <li><a href="/tasks/<%= task._id %>/edit"><%= task.title %></a></li>
                <% } %>
            </ul>
            <% } %>
        </section>
        <br><br><br><br>
        <button><a href="/tasks/create">Add new task</a></button>
    </main>
    
</body>
</html>